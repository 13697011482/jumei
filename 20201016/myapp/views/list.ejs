<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  hello list
  <div id="list">
    <% movieList.forEach((v,i) => { %>
      <a href="/detail/<%= v._id %>">
        <img src="<%= v.movieUrl %>" alt="">
        <p><%= v.movieName %></p>
      </a>
    <% }) %>
    
  </div>
</body>
</html>